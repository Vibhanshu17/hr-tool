import {CreateChatBotMessage, createChatBotMessage} from 'react-chatbot-kit'

const config = {
    botName: "Crafter Bot",
    initialMessages: [createChatBotMessage('Hi! Ask a question')],
}

export default config;